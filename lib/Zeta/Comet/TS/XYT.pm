package Zeta::Comet::TS::XYT;

use strict;
use warnings;
use POE;

use base qw/Zeta::Comet::TS/;

# sub on_remote_data {
#  $_[HEAP]{logger}->debug("XYT got remote data[$_[ARG0]]");
# }

# sub on_adapter_data {
#   my $class = shift;
#   $_[HEAP]{logger}->debug("XYT on_adapter_data called with:\n" . Data::Dump->dump($_[HEAP]));
#   $class->SUPER::on_adapter_data(@_);
# }

1;
